sudo: required
services:
  - docker

before_install:
  - docker build -t complex/client-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true complex/client-test npm test

after_success:
  - docker build -t complex/client ./client
  - docker build -t complex/nginx ./nginx
  - docker build -t complex/server ./server
  - docker build -t complex/worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push complex/client
  - docker push complex/nginx
  - docker push complex/server
  - docker push complex/worker
# deploy:
#   provider: elasticbeanstalk
#   region: "ap-southeast-1"
#   app: "docker-react"
#   env: "Dockerreact-env"
#   bucket_name: "elasticbeanstalk-ap-southeast-1-969768766207"
#   bucket_path: "docker-react"
#   on:
#     branch: main
#   access_key_id: $AWS_ACCESS_KEY
#   secret_access_key: $AWS_SECRET_KEY
